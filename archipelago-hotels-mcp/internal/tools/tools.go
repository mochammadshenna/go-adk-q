// Package tools — MCP tool registrations and handler functions.
//
// Each tool is registered in its own file with its handler closure.
// The DB pool is injected at registration time, not stored globally.
package tools
