# ── Binaries ──────────────────────────────────────────────────────────────────
/bin/
dist/
/adk-web
/pwdev-mcp
/pwdev-mcp-debug
adk-web-universal
pwdev-mcp-universal
/bootstrap
/lambda

# ── Lambda deployment artifacts ───────────────────────────────────────────────
main.zip
*.zip

# ── Build artifacts ───────────────────────────────────────────────────────────
*.exe
*.dll
*.so
*.dylib

# ── Test & coverage ───────────────────────────────────────────────────────────
/cover.out
/cover.html
*.test
*.bench

# ── Environment & secrets ─────────────────────────────────────────────────────
.env
.env.*
!.env.example
.envrc
.secrets

# ── IDE / editor ──────────────────────────────────────────────────────────────
.vscode/
# Real VS Code MCP config lives in ~/Library/Application Support/Code/User/mcp.json
# .vscode/ personal settings are local-only
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── MCP client configs (generated by make mcp-config — contain local paths)──
.cursor/
.continue/config.json
# .continue/mcpServers/ is intentionally tracked (YAML config for team)

# ── Agent session / handoff state ─────────────────────────────────────────────
.handoff_prompt.txt
session-summary.md
.claude_session/
*.claude-session

# ── Hot-reload ────────────────────────────────────────────────────────────────
tmp/
.air.toml

# ── GoReleaser output ─────────────────────────────────────────────────────────
dist/

# ── Profiling ─────────────────────────────────────────────────────────────────
*.prof
*.pprof
cpu.out
mem.out
trace.out

# ── Logs ──────────────────────────────────────────────────────────────────────
*.log
logs/

# ── Generated MCP client configs (contain absolute local paths) ───────────────
# Keep the templates in configs/mcp/ but ignore generated project-level files
.continue/config.json

# ── Genkit runtime state (local dev artifacts) ───────────────────────────────
.genkit/


# ── OS artifacts ──────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db